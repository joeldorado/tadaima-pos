# Handoff QA — 2026-06-25 (para Ruben + IA)

> **Rama de QA:** `dev/qa-handoff`, sincronizada con `main`/producción en rev **`tadaima-00097-9xb`**.
> **Detalle técnico completo:** `MASTERLOG.md` (entradas rev **00095**, **00096**, **00097** al inicio).
> Esta nota es el resumen scaneable de lo NUEVO + checklist de QA + contexto para la IA.

---

## 1. Cómo tomar los cambios

```bash
git checkout dev/qa-handoff
git pull origin dev/qa-handoff           # trae revs 00095 → 00097 (estabas en 00094)
cd backend && php artisan migrate        # corre la migración ADITIVA member_* (local SQLite)
# frontend: npm i si cambió algo, luego vite dev
```

- **PWA:** entre deploys el navegador cachea el bundle viejo. QA en **incógnito NUEVO** o **hard refresh (Cmd+Shift+R)**.
- **tsc rojo es NORMAL:** ~453 errores `exactOptionalPropertyTypes` PRE-EXISTENTES; no bloquean (el build usa `vite build`, salta tsc). No es regresión.

---

## 2. Qué hay nuevo

### 🧑‍🤝‍🧑 Socios Tadaima (rev 00097) — lo grande
- **Fix:** agregar un socio ya **no** da "Los datos enviados no son válidos" (422). Antes el nivel de
  membresía ("b") se mandaba como `loyalty_tier` y reventaba el enum.
- La **ficha del socio** ahora muestra **estatus** (Activo/Inactivo), nivel de membresía, vigencia y última sync.
- Se **sincroniza con Supabase** (solo lectura) al **abrir** la ficha en Clientes y al **asignar** el socio en Caja.
- **Precio socio (nivel b) solo aplica si está ACTIVO**; socio inactivo cobra precio normal + aviso.
- **Clientes:** filtro **Todos / Socios / Normales** + badge de estatus (verde activo / rojo inactivo) por fila.
- **Local (sin Supabase configurado):** el stub devuelve socios **ACTIVO** `TAD10000001..05` (busca "ana", "luis",
  "maria", etc.) → se puede QA el alta y el precio socio sin Supabase. **Inactivo solo se prueba en prod** (Supabase real).

### 🧾 Ticket 58 mm (rev 00097)
- Sale **VERTICAL** (antes salía de lado/horizontal). Imprime por **iframe oculto** (sin ventana emergente).
- **Impresora JP-58H aún NO en mano** → QA imprimiendo **a PDF**: debe verse angosto y vertical, con el correo/USD
  largos **partidos** (no cortados) y la fila **"Cambio"** cuando hay cambio.
- Para impresión **automática** (cuando llegue la impresora): impresora por defecto + Chrome `--kiosk-printing` + pref "auto".

### 🔔 Toasts (rev 00097)
- Ahora **siguen el tema dark/light**, tienen botón **✕** para cerrar, sombra (no se pierden) y **no se cortan a la derecha**.

### Rev 00096 (por si no lo tenías en 00094)
- Productos: **proveedor + categoría persisten** (antes se perdían) + drag&drop de imagen.
- Caja: **precio socio automático** (base de lo de 00097).
- Clientes: **upsert de socio** idempotente (re-asignar no truena).
- Recibo vertical (1ª pasada; la definitiva es 00097) · Bodegas: rename persiste.

### Rev 00095
- **Ventas no pierde el ticket** al refrescar (expansión en el padre + anclaje de scroll).
- Caja: **resumen de cobro USD/pesos** con falta/cambio claro + monto en el botón.

---

## 3. Checklist de QA (lo nuevo de esta tanda)

- [ ] Caja → **Asignar cliente** → buscar "mario"/"ana" → **Agregar** socio: se agrega **SIN** error 422.
- [ ] Socio **ACTIVO** asignado (no tarjeta, no preventa) → items a **precio socio (b)**; badge "Precio socio aplicado".
- [ ] Pagar con **Tarjeta** → vuelve a **precio normal** (la tienda absorbe la comisión).
- [ ] **Clientes:** filtro Socios/Normales funciona; badge verde/rojo por fila; ficha con bloque de socio + botón **"Actualizar estatus"**.
- [ ] Socio **INACTIVO** → precio normal + aviso (**solo en prod** con Supabase real).
- [ ] **Ticket a PDF** → vertical 58 mm, correo largo partido, fila **Cambio** cuando das cambio (efectivo).
- [ ] **Toasts** → en dark y light se ven bien; botón **✕** cierra; no se cortan a la derecha.

---

## 4. Notas para la IA (Codex / asistente de Ruben)

**Reglas para no romper lo nuevo:**
- **NO** mandar el nivel de membresía ("b") como `loyalty_tier` (enum `Bronce/Plata/Oro/Leyenda`) → da **422**.
  El nivel/estatus va en los campos nuevos: **`member_status` / `member_level` / `member_expires_at`** (en `customers`).
- **Snapshot del socio** en `customers`: `member_status / member_level / member_expires_at / member_debt / member_synced_at`
  (migración `2026_06_25_000001`). Se llena **solo lectura** desde Supabase vía `App\Services\TadaimaMemberService` y
  el endpoint **`POST /customers/{id}/refresh-member`**. **Nunca escribir a Supabase.**
- **Elegibilidad del descuento:** helper `isSocioEligible` en `SellPage.tsx` + toggle `SOCIO_ELIGIBILITY`
  (hoy solo `member_status==='ACTIVO'`). Separar **`Mesa.customerIsSocio`** (etiqueta) de **`Mesa.customerSocioEligible`** (precio).
- **Ticket:** `doPrintTicket` usa **iframe oculto** + `@page{size:58mm auto;margin:0}`. **NO** poner `orientation:portrait`
  suelto dentro de `@page` (es inválido y rota el ticket). Anchos en `mm`, `table-layout:fixed`.
- **Toasts:** configurar en **`components/AppToaster.tsx`** (`theme` del ThemeContext + `closeButton`) y **`styles/toast.css`**,
  no volver a hardcodear el `<Toaster>` en `App.tsx`.

**Archivos clave tocados esta tanda:**
- Backend: `CustomerController`, `ExternalCardController`, `Services/TadaimaMemberService` (nuevo),
  `migrations/2026_06_25_000001_add_member_snapshot_to_customers_table`, `Customer` model, `{Store,Update}CustomerRequest`,
  `CustomerResource`, `routes/api.php`, tests `CustomerSocioUpsertTest` + `CustomerRefreshMemberTest`.
- Shared: `packages/api/src/types.ts`, `packages/api/src/customers.ts` (`refreshMember`).
- Frontend: `landing/src/pages/SellPage.tsx`, `ClientsPage.tsx`, `hooks/queries/useCustomers.ts`,
  `App.tsx`, `components/AppToaster.tsx` (nuevo), `styles/toast.css` (nuevo), `styles/index.css`.

**Convenciones del repo:** ver `backend/AGENTS.md`. Backend con tests primero en caja/precios/cancelaciones/reportes.
Migraciones aditivas/idempotentes (corren solas en cada deploy). Deploy: `gcloud run deploy tadaima --source . --region us-central1`
**sin** flags de env (preserva los 22 secrets).
