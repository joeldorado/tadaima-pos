# URL del backend Laravel (sin /api/v1 — el cliente lo agrega solo).
#
# Opción A — QA contra PRODUCCIÓN (recomendado en fase de pruebas):
#   no necesitas correr Laravel ni ninguna base de datos local,
#   solo el frontend: `npm run dev` y listo.
# VITE_API_URL=https://tadaima.poslite.com.mx
#
# Opción B — backend local en :8000 (php artisan serve):
VITE_API_URL=http://127.0.0.1:8000
